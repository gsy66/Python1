capitalize（）:第一个字符大写
casefold（）：所有字符小写
center（width）：居中字符串，填充长度为width
count（sub，[start],[end]）:查找有多少个字符
endswith（sub，[start],[end]）：检查是否为某某结束true
startswith(sub，[start],[end]):检查是否以sub开头true
expandtabs([tablize=8]):把字符串中的\t符号转变为空格，默认是八
find(sub，[start],[end]):检测sub是否在字符串中，是返回索引，不在返回-1
isalnum():有数字或字母返回ture
isalpha():至少有一个字母且都是字母ture
isdecimal():十进制数ture
isdigit():只包含数字ture
islower():所有字符都是小写ture
isnumeric():只包含数字ture
isspace():只包含空格ture
istitle():字符是标题化（第一个字母大写）ture
isupper():所有字符都是大写ture
join(sub):以字符串作为分隔符，插入到以sub的字符之间
ljust(width):左对齐
rjust(width):右对齐
lower():变小写
upper():变大写
swapcase():翻转大小写
title（）：返回标题化字符
lstrip():去掉左边空格
rsrtip():去掉末尾空格
partition(sub):将字符串变成三个元组，中间用sub隔开
rpartition(sub):从右边开始查找partition
replace(old,new,[count]):替换
rfind（sub，[start],[end]）：从右侧查找
rindex(sub，[start],[end]):从右侧开始index（参数在列表中的位置）
split（sep=None,maxsplit=-1）:按照sub来切割
strip([chars]):默认去掉空格，也可以设置参数来删除两侧字符
translate(table)：按照对应关系替换
zfill(width):返回width的值，右对齐，前边用零填充。


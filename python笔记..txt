   删除数组的用法：
remove(   ) ：括号内直接写元素
del语句 ：    del   数组名[i]（能够删除整个列表）
pop(   )：默认最后一个，可以加上索引值。
   增加数组的用法：
append(   )：括号直接写元素
extend(   ):可以同时加两个。中间参数为列表：extend（【1,2】）
insert(   ):两个参数，第一个为数字，数组第一个为0：insert（1，插入）
   索引：
数组名[  :  ]'(原数组)
数组名[a:b](从a到b的元素)
   列表语句：
count:计算数据在列表中出现的次数
index：参数在列表中的位置  index（参数，起始位置，结束位置）
reverse:将数组数据前后翻转
sort：用指定的方式对列表排序：默认升序
    sort（func，key）
    sort(reverse=true):降序排列，默认为false
    元组
    重要内容为 ","例如（8,）为元组，（8）不是
  format:格式化，根据参数